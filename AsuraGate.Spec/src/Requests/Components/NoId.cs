namespace AsuraGate.Spec.Requests.Components;

/// <summary>
/// A marker for request objects that don't have an ID within the gw2 api to satisfy interface generics.
/// </summary>
public struct NoId
{
}